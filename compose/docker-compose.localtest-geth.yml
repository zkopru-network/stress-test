version: '3'

services:
  testnet:
    image: zkopru_testnet_geth
    build:
      context: ../
      dockerfile: ./dockerfiles/Contract.geth.dockerfile
    ports:
      - '5000:5000'
    labels:
      kompose.service.expose: testnet
    command: sh /proj/run_geth.sh
  coordinator:
    image: zkopru_node
    build:
      context: ../
      dockerfile: ./dockerfiles/Generator.dockerfile
    ports:
      - '8888:8888'
      - '5354:5354'
    depends_on:
      - 'testnet'
    labels:
      kompose.service.expose: coordinator
    command: >
      bash -c "nc -l -p 5354 &&
      node /generator/dist/coordinator.js"
  redis:
    image: redis
    ports:
      - '6379:6379'
    labels:
      kompose.service.expose: redis
  organizer:
    image: zkopru_node
    build:
      context: ../
      dockerfile: ./dockerfiles/Generator.dockerfile
    ports:
      - '8080:8080'
    depends_on:
      - 'redis'
      - 'coordinator'
    labels:
      kompose.service.expose: organizer
    command: bash -c "node /generator/dist/organizer.js"
  wallet:
    image: zkopru_node
    build:
      context: ../
      dockerfile: ./dockerfiles/Generator.dockerfile
    depends_on:
      - 'redis'
      - 'organizer'
    labels:
      kompose.controller.type: deployment
    command: >
      bash -c "sleep 10 && node /generator/dist/wallet.js"
  turner:
    image: zkopru_node
    build:
      context: ../
      dockerfile: ./dockerfiles/Generator.dockerfile
    depends_on:
      - 'wallet'
    command: bash -c "sleep 60 && node /generator/dist/blockturner.js"

